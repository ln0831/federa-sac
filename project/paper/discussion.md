# Discussion Draft

Status:
- drafted from the current completed evidence package

The most important finding of the current cycle is not that the FedGrid family "fails" in every setting. It is that the evidence is highly context-sensitive, and that the method story changes substantially once the evaluation is forced into deterministic, paired, context-aligned form and then checked again through a clean rerun. The historical main suite is negative, the clean rerun is weakly positive for `fedgrid_topo_proto`, and the corrected ablation is positive only for `fedgrid_topo_proto` while remaining negative for clustered distillation and its nearby variants.

This pattern suggests three practical interpretations. First, the clustered and prototype-aware mechanisms are not equally valuable: the current evidence favors prototype-sharing more than clustered distillation. Second, a method that looks attractive conceptually can still underperform if the added structure is not consistently aligned with the actual source of heterogeneity in the benchmark. Third, robustness-oriented variants may be probing a different part of the design space than the original distillation story. The positive single-seed `byzantine` result does not prove robustness superiority, but it does justify further mechanism-specific study.

The paper therefore benefits from a modest contribution statement. Rather than forcing a broad method-superiority claim, the manuscript can make a stronger and more credible contribution by showing how a reproducible paired protocol changes which conclusions survive scrutiny. If the fresh Q1 replica confirms the clean rerun, the method-positive route should still stay narrow and center on `fedgrid_topo_proto` rather than on the full clustered-distillation family.

Several limitations remain. The robustness suite is single-seed. Historical path drift is still visible in some legacy reports. The verified literature package is real but still incomplete. Most importantly, the live multi-seed ablation is not finished yet, so the current discussion cannot fully resolve whether distillation, gentler clustering, or no-distillation variants best explain the observed behavior. These limits should remain explicit in the final paper.
Several limitations remain. The robustness suite is single-seed. Historical path drift is still visible in some legacy reports. The verified literature package is real but still incomplete. Most importantly, the historical main suite and the clean rerun still disagree on the sign of `fedgrid_topo_proto`, so the current discussion cannot yet claim that the main benchmark has been fully resolved. These limits should remain explicit in the final paper.
