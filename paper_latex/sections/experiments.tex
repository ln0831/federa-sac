\section{Experiments}

The main benchmark is case141 with outage count $k=6$. The primary topology-shift context is \texttt{random\_reset}, while \texttt{static} serves as a matched in-distribution reference. The strongest current evidence packages are the completed main suite, the corrected multi-seed custom ablation suite, and the supporting robustness suite.

This section will be upgraded with finalized benchmark description, metrics, seed policy, and reproducibility details after the reviewer-risk and evidence maps are locked.
